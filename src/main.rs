fn main() {
    println!("Hello World again!");
    
    // Add a simple variable to store a number
    let number = 42;
    
    // Print the number
    println!("The answer to life, the universe, and everything is: {}", number);
    
    // Add a short loop to print a message twice
    for _ in 0..2 {
        println!("Learning Rust one step at a time!");
    }
}